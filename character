using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    public float ileriHiz = 10f; // Saniyede ne kadar ileri gideceği

    void Update()
    {
        transform.Translate(Vector3.forward * ileriHiz * Time.deltaTime);
    }
}
